// default values can be set here if values are changed to props

import React from "react";
import UserAttendeeComponent from "../UserAttendeeComponent";
import { Colors } from "../../../constants/colors";

export default <UserAttendeeComponent uxpId="UserAttendeeComponent"></UserAttendeeComponent>;
